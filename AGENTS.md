# Rebalance-tranching study

Preserve the distinction between averaging daily fixed-notional P&L and averaging
separately compounded portfolio paths. Do not change timing, cost or period definitions
while reorganizing code. Examples use invented inputs; saved evidence is portfolio-level.
This repository owns the mixture calculations, examples and timing figure generators.
The blog owns article prose and published SVG copies. Avoid duplicate renderers.
Keep public documentation about runnable methods and inputs, not internal projects or
unpublished work. Add only redistributable data and dependencies.

Run the four README checks and generation commands before committing. Validate input
hashes and reconcile the chart-ready CSV with daily portfolio evidence. Keep generated
output ignored. Preserve unrelated changes and stage explicit paths.
